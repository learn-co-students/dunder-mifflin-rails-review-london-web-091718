<h2><%= @dog.name %></h2>
<ul>
  <li>Breed: <%= @dog.breed %></li>
  <li>Age: <%= @dog.age %></li>
  <li>Employees: <br>
    <% @dog.employees.each do |emp| %>
      <%= link_to emp.to_s, employee_path(emp) %><br>
    <% end %>
  </li>
</ul>
