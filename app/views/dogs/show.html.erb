<h1><%=@dog.name%> Info ....</h1>


<ul>
  <li><%= @dog.name %></li>
  <li><%= @dog.breed %></li>
  <li><%= @dog.age %></li>
</ul>
<hr>
<h2> <%=@dog.name%> Employees...</h2>
  <ul>
    <% @dog.dog_emp.each do |e|%>
      <li><%= link_to e.first_name + " " + e.last_name, employee_path(e.id) %></li>
    <% end %>
  </ul>
